[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Nitro (preset: netlify) outputs public assets to dist/
# and functions to .netlify/functions-internal/
